<%= form_for(@tweet) do |field| %>
  <%= render 'shared/error_messages', object: field.object %>
  <div class="field">
    <%= field.text_area :content, placeholder: "Compose new tweet..." %>
  </div>
  <%= field.submit "Tweet", class: "btn btn-primary" %>
<% end %>
